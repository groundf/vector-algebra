/*
 * Stupidly simple implementation of vectors.
 */
#pragma once

#include <cstddef>

namespace gof {

template <std::size_t N>
class vector {
    // constructor
    vectorf(float x, float, y);
};

class vector3 {
    // constructor
    vector3(float x, float, y, float z);
}

class vector4 {
    //constructor
    vector4(float x, float y, float w);
}

// operators

template <typename N>
vec

} // namespace